import 'package:flutter/material.dart';

class ObrasScreen extends StatelessWidget {
  const ObrasScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Obras')));
}
