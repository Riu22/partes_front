import 'package:flutter/material.dart';

class PartesScreen extends StatelessWidget {
  const PartesScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Partes')));
}
