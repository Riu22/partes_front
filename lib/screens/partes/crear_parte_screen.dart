import 'package:flutter/material.dart';

class CrearParteScreen extends StatelessWidget {
  const CrearParteScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Crear parte')));
}
