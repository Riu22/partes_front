/// Colores fijos que se usan en toda la aplicación para mantener
/// un diseño uniforme. Aquí están definidos el color del fondo,
/// de las tarjetas, de los botones, alertas, etc.

import 'package:flutter/material.dart';

/// Color de fondo de las pantallas
const bgPage = Color(0xFFE8EAF0);
/// Color de fondo de las tarjetas
const bgCard = Colors.white;
/// Azul principal para textos y elementos destacados
const blue = Color(0xFF1565C0);
/// Fondo azul claro para etiquetas o "píldoras" informativas
const bluePill = Color(0xFFE3EDFF);
/// Naranja principal para elementos de advertencia o destacados
const orange = Color(0xFFF57C00);
/// Fondo naranja claro para etiquetas
const orangePill = Color(0xFFFFF3E0);
/// Color para chips de "electricidad" u obra
const chipElec = Color(0xFFF57C00);
/// Color de texto dentro de chips
const chipFont = Color(0xFF1565C0);
/// Color principal del texto
const textPrimary = Color(0xFF1A1A2E);
/// Color para textos secundarios o menos importantes
const textSecondary = Color(0xFF888888);
/// Color de los bordes de las tarjetas
const cardBorder = Color(0xFFE0E3EA);
/// Fondo de las tarjetas de estadísticas
const bgStat = Color(0xFFF1F3F8);
/// Verde para indicar que todo está correcto
const greenOk = Color(0xFF2E7D32);
/// Fondo verde claro para etiquetas de "ok"
const greenPill = Color(0xFFE8F5E9);
/// Rojo para alertas o errores
const redAlert = Color(0xFFC62828);
/// Fondo rojo claro para etiquetas de alerta
const redPill = Color(0xFFFFEBEE);
